# neostandard@0.13 peer range is still ^9; ESLint 10 works in practice.
legacy-peer-deps=true
# Mode strict pour éviter les hoisting surprises
shamefully-hoist=false
# Utiliser pnpm pour gérer les lockfiles
package-manager=pnpm
